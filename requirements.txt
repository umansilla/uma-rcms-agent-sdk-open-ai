# Avaya RCMS → OpenAI Realtime bridge
# Requires Python 3.11  (see runtime.txt / render.yaml)
# Versions pinned to avoid pip resolver timeout during Render builds.

# Web framework + ASGI server
fastapi==0.115.12
uvicorn[standard]==0.48.0

# OpenAI Agents SDK with Realtime API support
# openai-agents 0.8.4 requires openai>=2.19.0,<3 — do not pin openai separately
openai-agents[voice]==0.8.4

# JWT authentication (for Avaya's signed tokens)
PyJWT==2.10.1

# Load .env in local development (no-op on Render)
python-dotenv==1.1.0

# WebSocket transport for uvicorn
websockets==14.1
